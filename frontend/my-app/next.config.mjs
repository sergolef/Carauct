/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'cdn.pixabay.com',
            'vehicle-images.dealerinspire.com'
        ]
    }
};

export default nextConfig;
